package com.enderio.core.common.compat;

/**
 * By implementing this marker interface the class must have a static method by
 * the name of the constant contained within this interface
 */
public interface ICompat {
  public static final String METHOD_NAME = "load";
}
